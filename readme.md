Утилита командной строки для преобразования учебного конфигурационного языка в JSON согласно требованиям варианта №16.

📋 Требования задания
Входные/выходные данные:
Вход: файл (путь задаётся ключом --input)

Выход: JSON в стандартный вывод (stdout)

Поддерживаемый синтаксис (вариант 16):
Комментарии:

Однострочные: C комментарий

Многострочные: --[[ комментарий ]]

Числа: [+-]?\d+\.\d+ (например: 8080.0, -2.5, +3.14)

Словари:



{
    имя => значение,
    имя => значение
}
Имена: [_a-z]+ (только маленькие буквы и подчёркивание)

Строки: @"текст"

Константы:

Объявление: (define имя значение)

Использование: $имя$

Обязательные требования:
✅ Синтаксические ошибки выявляются с выдачей сообщений

✅ Все конструкции языка покрыты тестами

✅ 3 примера конфигураций из разных предметных областей

🚀 Установка и запуск
Установка:

# Клонировать репозиторий
git clone https://github.com/m1n1str/config-language-parser-v16

Зависимости:
Внешние зависимости отсутствуют. Требуется только Python 3.7+.

Использование:

# Преобразовать конфигурацию в JSON (вывод в stdout)
python cli.py --input example_server.conf

# Сохранить результат в файл
python cli.py --input example_server.conf > output.json

# Включить отладочный вывод
python cli.py --input example_server.conf --debug
Структура проекта

config-parser-v16/
├── parser.py              # Основной парсер конфигураций

├── cli.py                 # Интерфейс командной строки

├── test_parser.py         # Автоматические тесты

├── example_server.conf    # Пример 1: конфигурация веб-сервера

├── example_game.conf      # Пример 2: конфигурация игры

├── example_app.conf       # Пример 3: конфигурация мобильного приложения

├── README.md              # Документация (этот файл)

├── .gitignore             # Игнорируемые файлы


└── requirements.txt       # Зависимости (пустой)

Примеры использования
Пример 1: Веб-сервер
python cli.py --input example_server.conf


Пример 2: Игра
python cli.py --input example_game.conf


Пример 3: Мобильное приложение
python cli.py --input example_app.conf



 !Тестирование!
Запуск всех тестов:
python test_parser.py

Покрытие тестами:
Базовые конструкции (словари, числа, строки)

Комментарии (однострочные и многострочные)

Константы (define и использование)

Крайние случаи (отрицательные числа, сложные идентификаторы)

Реальные примеры (все три конфигурационных файла)

Пример тестового вывода:
ЗАПУСК ТЕСТОВ ДЛЯ ПАРСЕРА КОНФИГУРАЦИЙ (ВАРИАНТ 16)
============================================================
>>> ЗАПУСК СЮИТЫ: Базовые
✓ Все базовые тесты пройдены
>>> ЗАПУСК СЮИТЫ: Комментарии
✓ Все тесты комментариев пройдены
>>> ЗАПУСК СЮИТЫ: Константы
✓ Все тесты констант пройдены
>>> ЗАПУСК СЮИТЫ: Крайние случаи
✓ Все крайние случаи обработаны
>>> ЗАПУСК СЮИТЫ: Реальные примеры
✓ Все реальные примеры обработаны
============================================================
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
📚 Синтаксис учебного языка
Базовый пример:

{
    port => 8080.0,
    host => @"localhost",
    debug => true
}
С константами:

--[[ Объявление констант ]]
(define server_port 8080.0)
(define server_host @"localhost")
>>>
>>>{
    port => $server_port$,
    host => $server_host$
}
Вложенные словари:

{
    server => {
        port => 80.0,
        host => @"0.0.0.0"
    },
    database => {
        host => @"localhost",
        port => 5432.0
    }
}
С комментариями:

C Это однострочный комментарий
{
    port => 8080.0,  C Порт сервера
    
    --[[
    Многострочный
    комментарий
    ]]
    host => @"localhost"
}

🔧 Обработка ошибок
Парсер выявляет следующие типы ошибок с указанием строки и позиции:

Синтаксические ошибки:
Неправильные идентификаторы (начинаются не с _ или маленькой буквы)

Отсутствие => между ключом и значением

Непарные фигурные скобки { }

Неправильный формат чисел

Незакрытые строки

Семантические ошибки:
Неопределённые константы

Повторное определение констант (разрешено, используется последнее значение)

Некорректные значения в define

Пример сообщения об ошибке:
Синтаксическая ошибка: Неправильный идентификатор: Server (строка 3, позиция 5)
Ожидался идентификатор, начинающийся с _ или маленькой буквы
📊 Результаты выполнения требований
Требование	Статус	Комментарий
Вход из файла (--input)	✅ Выполнено	Поддерживается аргументом --input
Выход в JSON в stdout	✅ Выполнено	Результат выводится в стандартный вывод
Синтаксические ошибки с сообщениями	✅ Выполнено	Подробные сообщения с указанием строки
Поддержка синтаксиса варианта 16	✅ Выполнено	Все конструкции языка реализованы
Константы (define и $...$)	✅ Выполнено	Полная поддержка объявления и использования
Тестовое покрытие	✅ Выполнено	5 тестовых наборов, 40+ отдельных тестов
3 примера из разных областей	✅ Выполнено	Сервер, игра, мобильное приложение
🚀 Развёртывание в Git
История коммитов(возможно не соотвествие с количеством комитов на гите):
feat: basic parser structure
feat: add parser for dictionaries and basic values  
feat: add constants support (define and $...$)
feat: add CLI interface and validation
feat: add comprehensive tests and documentation
feat: add third example configuration (mobile app)
feat: improve parser reliability and error handling


Требования к репозиторию:
Публичный доступ

Детальные сообщения коммитов

README.md с документацией

Тесты и примеры

📞 Поддержка и обратная связь
Известные ограничения:
Не поддерживаются экранированные кавычки в строках (\")

Булевы значения true/false не являются частью спецификации, но обрабатываются

Комментарии внутри строк не поддерживаются

Возможные улучшения:
Поддержка массивов (не требуется по варианту 16)

Более сложные вычисления констант

Валидация схемы конфигурации

Поддержка импорта других файлов

👥 Автор 
Автор:Нестеренко Матвей Андреевич Студент ИКБО-70-24
Дисциплина: Конфигурационное управление
Семестр: 3 семестр (осенний) 2025/2026 учебного года
Проект выполнен в рамках домашнего задания по дисциплине "Конфигурационное управление". Все требования варианта №16 выполнены в полном объёме.
